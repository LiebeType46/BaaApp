# B&A App TODO

## 優先度高

- [ ] パスワード変更APIの実機確認
  - アプリ: 設定 > アカウント設定 > パスワード変更
  - サーバー: `POST /auth/password/change`
  - 成功時に新しいtokenが保存されることを確認する
- [x] 同一端末でアカウントを切り替えた場合のローカルデータ分離
  - `locations` にユーザーIDを持たせる
  - `search_condition` をユーザー別に分ける
  - 未所有データをログイン/登録ユーザーへ紐づける
  - 写真ファイルはDBの所有者分離で参照範囲を分ける
- [ ] サーバー通信設定の実処理
  - [ ] 通信設定の保存
  - [x] 手動同期実行
  - [x] 同期結果のエラー表示
  - [x] 写真本体の同期
  - [ ] 写真同期失敗一覧の表示
- [ ] 自動同期・リトライ制御
  - [ ] 投稿直後同期
  - [ ] アプリ起動時同期
  - [ ] アプリ復帰時同期
  - [ ] 失敗後の予約再試行
  - [ ] 定期同期は低頻度の保険として検討
  - [ ] ネットワーク復帰時同期は電池消費が大きいため初期実装では見送り
  - [ ] 同期の多重起動防止
  - [ ] 前回同期時刻による短時間連続実行の抑制
  - [ ] サーバーが返す `retryAfterSeconds` / `nextSyncAt` を保存して次回同期時刻を制御する
  - [ ] 自動同期はサーバー指定時刻まではスキップする
  - [ ] 手動同期時はサーバー制限中であることを表示する

## 設定画面

- [ ] 全般設定
  - [ ] 言語設定の実保存
  - [ ] 地図タイルキャッシュ削除の実処理
  - [ ] アプリ情報の表示内容整理
- [ ] アカウント設定
  - [ ] ユーザー情報編集
  - [ ] ログアウト処理
- [ ] データ管理
  - [ ] CSVエクスポート
  - [ ] CSVインポート後の結果表示

## 検索

- [ ] 検索条件の複数保存
  - 名前付き保存
  - プルダウン選択
- [ ] 他ユーザー投稿テーブルへの検索条件流用
- [ ] 地図とタイムラインの検索結果件数・状態表示

## サーバー

- [ ] 同期リクエスト制限
  - [ ] 1ユーザー1同期などの軽い制限
  - [ ] 1回あたりの地点件数・写真サイズ上限
  - [ ] 制限時はHTTP `429 Too Many Requests` を返す
  - [ ] `Retry-After` ヘッダーを返す
  - [ ] JSON本文に `retryAfterSeconds` / `nextSyncAt` を含める
  - [ ] 将来的にDBベースの同期ジョブキューを検討
  - [ ] 必要になったらRedis/RabbitMQ等の外部キューを検討
- [ ] デプロイ手順の整理
  - systemd化
  - restart手順の固定
  - ログ確認手順の固定
- [ ] 認証まわりの安全性強化
  - パスワード変更後の既存token失効
  - ログイン失敗回数制限
  - 監査ログ

## 保留・検討

- [ ] OSMタイル利用ポリシーに沿った長期対応
- [ ] 画面・処理関係図の更新
- [ ] アプリ内文言の多言語ファイル経由漏れチェックの定期化
